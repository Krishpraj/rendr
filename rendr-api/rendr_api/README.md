# Rendr API

## Getting Started

1. install dependencies: `pip install -e .`
2. run the server: `uvicorn rendr_api.main:app --reload`
3. open the browser and go to `http://localhost:8000/docs` to see the API documentation and test the endpoints.
